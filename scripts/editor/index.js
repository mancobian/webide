define([ 'jquery', 'underscore', 'angular', 'text!editor/layout.partial' ],
function( $, _, Angular, Layout ) {

var initialize = function() {
};

return {
  initialize: initialize,
};

}); // end define()
